# 手机优先适配

本版优先安卓横屏，沿用单人生存模式。Android/iOS 运行时自动启用触控；桌面可通过 `MobilePreview.bat` 预览。

## 类似吃鸡手游的操作

- 左摇杆控制移动，上推超过 86% 自动疾跑，松手停止。
- 右侧空白区域滑动转视角。
- 右侧大开火键按住连射，拖动该手指可同时瞄准/压枪。
- 左侧副开火键方便三指操作；两根开火手指同时按住时，松开一根不会提前停止射击。
- 点按开镜、点按蹲下切换状态；跳跃、换弹、治疗、拾取独立按键。
- 右上暂停键；暂停、切到后台、取消触摸时释放所有摇杆/开火状态，避免粘键。
- 暂停页灵敏度控制同步影响触屏视角。当前不含陀螺仪、趴下、探头、自定义拖拽键位或辅助瞄准。

## 屏幕与性能

- 横屏和扩展宽高比；逻辑 1280×720 控件居中缩放，原生设备使用安全显示区域，避开刘海。
- 手机血条移到下方中央，弹药移到右上；桌面键盘提示隐藏，按钮与准星分开。
- 手机关闭 MSAA，3D 渲染比例 80%，帧率上限 60；阴影范围 65 米单分区，缩减草地和海面网格，并限制场景细节可见距离。
- AI 通行图分帧构建，避免单次长时间阻塞初始化。玩法敌人数量仍为 8。
- 以上是画质配置，不代表已在手机上达到稳定 60 FPS；需要真机测试耗电、温度、触控延迟和不同 GPU 兼容性。

## 电脑预览

双击 `MobilePreview.bat`。用鼠标按住/拖动手机按钮或右侧画面；单只鼠标只能模拟一根手指，完整多点触控使用原生触屏。桌面普通 `Launch.bat` 仍保留键鼠模式。

自动触屏测试使用 Godot 的 ScreenTouch / ScreenDrag 事件，并非直接调用角色移动函数：

```bat
tools\godot\Godot_v4.5.1-stable_win64_console.exe --path . --resolution 1600x720 --max-fps 60 --script res://tests/mobile_input.gd -- --mobile-preview --mobile-test
```

测试记录 `tests/mobile_results.json`；画面 `tests/mobile_01_controls.png`。

## Android APK 导出状态

已添加 `export_presets.cfg` 的 Android arm64 预设，但**尚未生成或验证 APK**。

当前这台电脑缺少以下必要组件：

1. JDK 17（目前配置为 JDK 8）。
2. Android SDK 和对应构建工具（编辑器配置的 SDK 目录不存在）。
3. 与 Godot 4.5.1 匹配的 Android 导出模板（模板目录为空）。

准备这些组件后，在 Godot 编辑器设置中配置 Java SDK / Android SDK，安装 4.5.1 导出模板，创建调试签名密钥，然后选择项目 → 导出 → Android。首次建议导出调试 APK 到 `build/CoastlineProtocol.apk`，在安卓真机安装并验证多点触控。正式发布必须单独配置自己的签名密钥。

没有修改电脑的 Java/Android 全局配置，没有安装 SDK，也没有将桌面预览冒充手机实测。
